int num = 2;

~ATH(num <= 10){  // Ciclo continuará enquanto num <= 10
    // Dentro de um ciclo de vida apenas novos ciclos
    // Ciclo abaixo irá apenas incrementar num em 4
    ~ATH(){} EXECUTE ( num += 4; );
} EXECUTE (NULL); // Ao final não faz nada

THIS->DIE(); // Finalizar